<resources>

    <!-- Base application theme. -->
    <style name="itemMargin">
        <item name="android:layout_marginTop">@dimen/margin_size_10dp</item>
        <item name="android:layout_marginLeft">@dimen/marginlr</item>
        <item name="android:layout_marginRight">@dimen/marginlr</item>
        <item name="android:textSize">@dimen/textSize</item>
    </style>

</resources>
